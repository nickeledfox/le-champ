import tw, { styled, css } from 'twin.macro';

import { Hero, Spa } from '../components/containers/home';

export const Home = () => {
  return (
    <div>
      <Hero />
      <Spa />
    </div>
  );
};
